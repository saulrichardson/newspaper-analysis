[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "newspaper-analysis"
version = "0.2.0"
description = "Evidence-first offline analysis for fused newspaper parsing artifacts."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
newspaper-analysis = "newsvlm_analysis.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["archive", "artifacts", "reports", ".venv"]
