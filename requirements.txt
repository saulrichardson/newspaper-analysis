# Optional scientific stack for frontier corpus, clustering, and report work.
# The canonical offline analysis run uses only the dependency-free package in
# pyproject.toml and does not require this file.

# Install this repo as a package (so scripts can import `newsvlm_analysis.*`).
-e .

numpy
pydantic>=2
pandas
scikit-learn
statsmodels
pyarrow
matplotlib
umap-learn
hdbscan
openpyxl
